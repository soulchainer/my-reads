// An image to show when there isn't an available image for the actual book.
import defaultCover from '../assets/defaultCover.svg';

// An object with shelves names as keys and their asociated labels as values
export const SHELVES = {
  'currentlyReading': 'Currently Reading',
  'wantToRead': 'Want to Read',
  'read': 'Read',
  'none': 'None'
};

export const COVER = defaultCover;
